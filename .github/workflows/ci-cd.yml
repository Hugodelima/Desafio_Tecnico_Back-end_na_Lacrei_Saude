name: CI/CD Pipeline - Lacrei Saúde (Amazon Linux Only)

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  DJANGO_SECRET_KEY: ${{ secrets.DJANGO_SECRET_KEY }}
  DEBUG: 'False'
  POSTGRES_DB: test_db
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres
  DATABASE_HOST: localhost
  DATABASE_PORT: 5432
  CI: 'true'
  GITHUB_ACTIONS: 'true'
  ALLOWED_HOSTS: 'localhost,127.0.0.1,0.0.0.0,test-server'

jobs:
  test:
    name: Run Tests with SSL
    runs-on: ubuntu-latest
    environment: test
    
    services:
      postgres:
        image: postgres:13
        env:
          POSTGRES_DB: test_db
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libpq-dev gcc openssl

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install --no-cache-dir -r requirements.txt

    - name: Generate SSL certificates for testing
      run: |
        echo "🔐 Gerando certificados SSL para testes..."
        openssl req -x509 -newkey rsa:4096 -nodes \
          -out cert.pem \
          -keyout key.pem \
          -days 1 \
          -subj "/C=BR/ST=Test/L=Test/O=Test/CN=localhost" \
          -addext "subjectAltName=DNS:localhost,IP:127.0.0.1"
        echo "✅ Certificados SSL gerados"

    - name: Wait for PostgreSQL
      run: |
        for i in {1..10}; do
          if pg_isready -h localhost -p 5432 -U postgres; then
            echo "✅ PostgreSQL pronto!"
            break
          fi
          echo "Aguardando PostgreSQL... tentativa $i"
          sleep 3
        done

    - name: Run migrations and tests
      run: |
        python manage.py migrate --no-input
        python manage.py test --verbosity=2 --no-input

  setup-ec2-environment:
    name: Setup EC2 Environment
    runs-on: ubuntu-latest
    environment: test
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Staging EC2 Directory
      uses: appleboy/ssh-action@v0.1.6
      with:
        host: ${{ secrets.STAGING_EC2_HOST }}
        username: ${{ secrets.EC2_USERNAME }}
        key: ${{ secrets.STAGING_EC2_SSH_KEY }}
        script: |
          echo "🔧 Configurando ambiente STAGING EC2..."
          echo "📂 Diretório atual: $(pwd)"
          
          # Criar diretório se não existir
          sudo mkdir -p /app/lacrei-api
          sudo chown ec2-user:ec2-user /app/lacrei-api
          echo "✅ Diretório /app/lacrei-api criado"
          
          # Verificar se tem git clone
          if [ ! -d "/app/lacrei-api/.git" ]; then
              echo "📦 Clonando repositório..."
              cd /app/lacrei-api
              git clone https://github.com/Hugodelima/Desafio_Tecnico_Back-end_na_Lacrei_Saude.git .
              echo "✅ Repositório clonado"
          else
              echo "✅ Repositório já existe"
          fi
          
          echo "🎉 Ambiente STAGING configurado!"

    - name: Setup Production EC2 Directory
      uses: appleboy/ssh-action@v0.1.6
      with:
        host: ${{ secrets.PRODUCTION_EC2_HOST }}
        username: ${{ secrets.EC2_USERNAME }}
        key: ${{ secrets.PRODUCTION_EC2_SSH_KEY }}
        script: |
          echo "🔧 Configurando ambiente PRODUCTION EC2..."
          echo "📂 Diretório atual: $(pwd)"
          
          # Criar diretório se não existir
          sudo mkdir -p /app/lacrei-api
          sudo chown ec2-user:ec2-user /app/lacrei-api
          echo "✅ Diretório /app/lacrei-api criado"
          
          # Verificar se tem git clone
          if [ ! -d "/app/lacrei-api/.git" ]; then
              echo "📦 Clonando repositório..."
              cd /app/lacrei-api
              git clone https://github.com/Hugodelima/Desafio_Tecnico_Back-end_na_Lacrei_Saude.git .
              echo "✅ Repositório clonado"
          else
              echo "✅ Repositório já existe"
          fi
          
          echo "🎉 Ambiente PRODUCTION configurado!"

  deploy-staging:
    name: Deploy to Staging EC2
    runs-on: ubuntu-latest
    needs: [test, setup-ec2-environment]
    if: github.ref == 'refs/heads/develop'
    environment: test
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Deploy to Staging EC2
      uses: appleboy/ssh-action@v0.1.6
      with:
        host: ${{ secrets.STAGING_EC2_HOST }}
        username: ${{ secrets.EC2_USERNAME }}
        key: ${{ secrets.STAGING_EC2_SSH_KEY }}
        script: |
          echo "🚀 Deploying to STAGING EC2..."
          echo "📂 Diretório atual: $(pwd)"
          
          # IR PARA O DIRETÓRIO CORRETO - AJUSTE CRÍTICO!
          cd /app/lacrei-api
          echo "📂 Agora no diretório: $(pwd)"
          ls -la
          
          # Atualizar código
          echo "🔄 Atualizando código..."
          sudo git fetch origin develop
          sudo git reset --hard origin/develop
          sudo git clean -fd
          echo "✅ Código atualizado"
          
          # Configurar SSL se necessário
          if [ ! -f "cert.pem" ] || [ ! -f "key.pem" ]; then
              echo "🔐 Gerando certificados SSL..."
              sudo openssl req -x509 -newkey rsa:4096 -nodes \
                -out cert.pem \
                -keyout key.pem \
                -days 365 \
                -subj "/C=BR/ST=Sao_Paulo/L=Sao_Paulo/O=Lacrei_Saude/CN=localhost"
              echo "✅ Certificados SSL gerados"
          else
              echo "✅ Certificados SSL já existem"
          fi
          
          # Reconstruir containers
          echo "🐳 Reiniciando containers..."
          docker-compose down || true
          docker-compose up --build -d
          
          echo "✅ Staging deploy complete!"
          echo "📊 Status dos containers:"
          docker-compose ps
          echo "📝 Logs recentes:"
          docker-compose logs web --tail=10

  deploy-production:
    name: Deploy to Production EC2
    runs-on: ubuntu-latest
    needs: [test, setup-ec2-environment]
    if: github.ref == 'refs/heads/main'
    environment: test
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Deploy to Production EC2
      uses: appleboy/ssh-action@v0.1.6
      with:
        host: ${{ secrets.PRODUCTION_EC2_HOST }}
        username: ${{ secrets.EC2_USERNAME }}
        key: ${{ secrets.PRODUCTION_EC2_SSH_KEY }}
        script: |
          echo "🚀 Deploying to PRODUCTION EC2..."
          echo "📂 Diretório atual: $(pwd)"
          
          # IR PARA O DIRETÓRIO CORRETO - AJUSTE CRÍTICO!
          cd /app/lacrei-api
          echo "📂 Agora no diretório: $(pwd)"
          ls -la
          
          # Atualizar código
          echo "🔄 Atualizando código..."
          sudo git fetch origin main
          sudo git reset --hard origin/main
          sudo git clean -fd
          echo "✅ Código atualizado"
          
          # Configurar SSL se necessário
          if [ ! -f "cert.pem" ] || [ ! -f "key.pem" ]; then
              echo "🔐 Gerando certificados SSL..."
              sudo openssl req -x509 -newkey rsa:4096 -nodes \
                -out cert.pem \
                -keyout key.pem \
                -days 365 \
                -subj "/C=BR/ST=Sao_Paulo/L=Sao_Paulo/O=Lacrei_Saude/CN=localhost"
              echo "✅ Certificados SSL gerados"
          else
              echo "✅ Certificados SSL já existem"
          fi
          
          # Reconstruir containers
          echo "🐳 Reiniciando containers..."
          docker-compose down || true
          docker-compose up --build -d
          
          echo "✅ Production deploy complete!"
          echo "📊 Status dos containers:"
          docker-compose ps
          echo "📝 Logs recentes:"
          docker-compose logs web --tail=10

  health-check:
    name: Health Check after Deploy
    runs-on: ubuntu-latest
    needs: [deploy-staging, deploy-production]
    if: always()
    
    steps:
    - name: Check Staging Health
      if: needs.deploy-staging.result == 'success'
      uses: appleboy/ssh-action@v0.1.6
      with:
        host: ${{ secrets.STAGING_EC2_HOST }}
        username: ${{ secrets.EC2_USERNAME }}
        key: ${{ secrets.STAGING_EC2_SSH_KEY }}
        script: |
          echo "🏥 Health Check - STAGING"
          echo "📂 Diretório: $(pwd)"
          cd /app/lacrei-api
          sleep 10
          curl -k https://localhost:8000/api/ || echo "⚠️ Staging health check failed - pode estar inicializando"
          docker-compose ps
          docker-compose logs web --tail=5

    - name: Check Production Health
      if: needs.deploy-production.result == 'success'
      uses: appleboy/ssh-action@v0.1.6
      with:
        host: ${{ secrets.PRODUCTION_EC2_HOST }}
        username: ${{ secrets.EC2_USERNAME }}
        key: ${{ secrets.PRODUCTION_EC2_SSH_KEY }}
        script: |
          echo "🏥 Health Check - PRODUCTION"
          echo "📂 Diretório: $(pwd)"
          cd /app/lacrei-api
          sleep 10
          curl -k https://localhost:8000/api/ || echo "⚠️ Production health check failed - pode estar inicializando"
          docker-compose ps
          docker-compose logs web --tail=5